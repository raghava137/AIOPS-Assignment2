apiVersion: v1
kind: Service
metadata:
  name: spam-api-svc
spec:
  type: NodePort
  selector:
    app: spam-api      # matches the Deployment's pod labels
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30081
